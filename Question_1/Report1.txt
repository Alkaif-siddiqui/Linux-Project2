1. command : nano submsission_backup.sh
   observation: it open nano text editor, it is used to write shell script and modify the 
   script.whenever open the editor i write the script and press ctrl + o , then press Enter to
   save it then i press ctrl + x to exit the editor.  

2. command: chmod +x submsission_backup.sh
   observtaion: Normally file have only read and write permission but after this command file
   have execute permission so, that i run the script file (submsission_backup.sh).

3. command: mkdir submissions backup
   observation: It creates two directory in persent working directory , their names submissions
   and backup.
 
4. command: touch script_report.txt errors.log
   observation: It creates two file one is text file named as script_report.txt where redirects 
   the ouput of script code output and second one is errors.log where redirects error of that
   script code.

5. command: ./submission_backup.sh
   output : Task Completed
   observation: its run the shell script code which is written in that shell script file.
   its redirects error on errors.log file and outut of this shell script redirects on 
   script_report.txt file.
   
   

