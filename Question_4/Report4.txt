1. command: touch system.log 
   observation: It creates a log file where i add entries using echo command and the perform operation.

2. command: touch Report4.txt
   observation: It creates a text file. where we Explain the every command output, we write here what I observed after 
   executing every command.

3. command : echo "message">>system.log
   observation: The echo command prints the messgae which is written between double qoute (""). The >> redirects 
   input or add entries  in the file which u gave after this operator >>

4. command: a)ls -l error_report.txt b)chmod +x error_report.txt
   output: a)-rw-rw-rw- 1 codespace codespace 0 Jul 21 10:02 error_report.txt
   observation: a)This command uses to check wherther file have only read, write, execute and all permitted or not.
                b) This command is use tp change the file permission to execute.


   