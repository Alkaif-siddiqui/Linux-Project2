1. command: nano main.c
   observtaion: It creates C file using nano editor and where i write progaram of creates file , open() , update, 
   read(), Write() and close ().

2. command:touch Report3.txt
   observtaion: It creates a text file named a  Report3.txt Where i  Write the observtaion of command and 
   explainations of Q3.

3.command: a)gcc main.c -o file b)./file
  a)This command compile the code which written in file name main.C
  b) This command run the program and output is:
     ID = 102
     Name = Bob
     Salary = 65000.00

4.Explaination:
  a)open(): creates or open the employee file name is employee.dat, retruns the file descriptor.
  b)write(): Write the employee records. Here we writes only 3 emloyee records only. and also update second emloyee 
    record without rewriting whole file.
  c)lseek(): Locates the second record by moving the file pointer, enabling direct access.
  d)write(): Retrieve the modified second employee entry from the designated file.
  e)close(): This closes the file and frees the file descriptor for future allocation.

  


  


